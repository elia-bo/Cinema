@model Cinema.Domain.Assegnamento

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>Assegnamento</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="IdSala" class="control-label"></label>
                <select asp-for="IdSala" class ="form-control" asp-items="ViewBag.IdSala"></select>
            </div>
            <div class="form-group">
                <label asp-for="IdSpettatore" class="control-label"></label>
                <select asp-for="IdSpettatore" class ="form-control" asp-items="ViewBag.IdSpettatore"></select>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
